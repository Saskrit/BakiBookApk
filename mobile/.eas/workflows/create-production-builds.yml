name: Create Production Builds

jobs:
  build_android_aab:
    name: Build Android AAB (Play Store)
    type: build
    params:
      platform: android
      profile: production

  build_android_apk:
    name: Build Android APK (Direct Install)
    type: build
    params:
      platform: android
      profile: apk
